"""Framework integrations for sesmio.

Each sub-module is safe to import even when the corresponding framework is not
installed — it will raise ``ImportError`` only when the class is instantiated
or a function is called.
"""
